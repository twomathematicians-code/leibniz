# Leibniz Engine — CORE requirements (lightweight; no ML stack needed)
# The full pipeline runs end-to-end on these alone via the StubBackend.
pydantic>=2
pyyaml>=6

# --- Symbolic computation (Wolfram-Alpha-style compute layer) ---
sympy>=1.12

# --- API host (api/) ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Streamlit Cloud deployment ---
streamlit>=1.40
pdfplumber>=0.11
pandas>=1.5

# --- Gradio app (app/) ---
gradio>=4

# --- Training / HF model inference (install separately) ---
# torch>=2.1
# transformers>=4.40
# peft>=0.10
# datasets>=2.18
# accelerate>=0.25
